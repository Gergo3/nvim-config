name: CI/CD

on:
  push:
    branches:
      - master
    tags:
      - 'v*.*'
      - 'v*.*.*'

permissions:
  contents: write


jobs:
  ci:
    uses: Gergo3/nvim-docker-workflows/.github/workflows/ci.yml
    secrets: inherit
